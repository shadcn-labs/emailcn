{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "email-divider",
  "title": "Email Divider",
  "description": "Horizontal divider for email templates",
  "dependencies": [
    "react-email"
  ],
  "registryDependencies": [
    "https://emailcn.com/r/email-theme-default.json"
  ],
  "files": [
    {
      "path": "registry/bases/react-email/ui/divider.tsx",
      "content": "import { Section, Tailwind, Text } from \"react-email\";\n\nimport { defaultTheme } from \"@/registry/themes/default\";\nimport type { EmailTheme } from \"@/registry/themes/define\";\n\nexport interface DividerProps {\n  theme?: EmailTheme;\n  label?: string;\n}\n\nexport const Divider = ({ theme = defaultTheme, label }: DividerProps) => (\n  <Tailwind config={theme}>\n    <Section className=\"py-6\">\n      {label ? (\n        <Text className=\"text-center text-sm text-foreground-muted\">\n          {label}\n        </Text>\n      ) : (\n        <Section className=\"border-b border-border\" />\n      )}\n    </Section>\n  </Tailwind>\n);\n\nDivider.PreviewProps = {\n  label: \"— or —\",\n  theme: defaultTheme,\n} satisfies DividerProps;\n",
      "type": "registry:component"
    }
  ],
  "categories": [
    "email-component"
  ],
  "type": "registry:component"
}