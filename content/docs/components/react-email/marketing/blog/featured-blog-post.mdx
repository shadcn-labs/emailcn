---
title: "Featured Blog Post"
description: "A Mailviews blog layout implemented with shared email-safe primitives."
---

This public component retains its Mailviews content contract while reusing the
same renderer-specific blog primitives as the rest of the family.

## Examples

### Default

<ComponentPreview base="react-email" name="featured-blog-post-demo" />

## Installation

<CodeTabs>
<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @emailcn/featured-blog-post
```

</TabsContent>
<TabsContent value="manual">

<Steps>
  <Step>Install the renderer dependency:</Step>

```bash
npm install react-email
```

<Step>Copy the shared blog primitives into your project.</Step>
<ComponentSource
  src="registry/bases/react-email/ui/marketing/blog/blog-shared.tsx"
  base="react-email"
  title="components/emails/marketing/blog/blog-shared.tsx"
/>

  <Step>Copy the component source into your project.</Step>
  <ComponentSource
    src="registry/bases/react-email/ui/marketing/blog/featured-blog-post.tsx"
    base="react-email"
    title="components/emails/marketing/blog/featured-blog-post.tsx"
  />
</Steps>

</TabsContent>
</CodeTabs>

## Usage

```tsx
import { FeaturedBlogPost } from "@/components/emails/marketing/blog/featured-blog-post";
```

```tsx
<FeaturedBlogPost {...FeaturedBlogPost.PreviewProps} />
```

## API Reference

| Prop                      | Type             | Default         | Description                                     |
| ------------------------- | ---------------- | --------------- | ----------------------------------------------- |
| `theme`                   | `TailwindConfig` | `defaultTheme`  | Renderer theme configuration.                   |
| `heading`                 | `string`         | Layout-specific | Optional section heading where supported.       |
| `imageSrc*` / `imageAlt*` | `string`         | Example imagery | Post image sources and labels.                  |
| `title*` / `excerpt*`     | `string`         | Example copy    | Post titles and supporting excerpts.            |
| Metadata props            | `string`         | Layout-specific | Author, host, episode, badge, or date metadata. |
