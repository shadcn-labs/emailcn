---
title: "Background Image"
description: "A background-image CTA with flush, boxed-overlay, and padded-card variants"
---

<ComponentPreview base="react-email" name="cta-with-background-image-demo" />

## Installation

<CodeTabs>
<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @emailcn/cta-with-background-image
```

</TabsContent>
<TabsContent value="manual">

<Steps>
  <Step>Copy the component source into your project.</Step>
  <ComponentSource
    src="registry/bases/react-email/ui/marketing/cta/cta-with-background-image.tsx"
    base="react-email"
    title="components/emails/marketing/cta/cta-with-background-image.tsx"
  />
</Steps>

</TabsContent>
</CodeTabs>

## Usage

```tsx
import { CTAWithBackgroundImage } from "@/components/emails/marketing/cta/cta-with-background-image";
```

```tsx
<CTAWithBackgroundImage {...CTAWithBackgroundImage.PreviewProps} />
```

## Examples

### Flush

<ComponentPreview
  base="react-email"
  name="cta-with-background-image-demo"
  variant="flush"
  hideCode
/>

### Boxed

<ComponentPreview
  base="react-email"
  name="cta-with-background-image-demo"
  variant="boxed"
  hideCode
/>

### Padded

<ComponentPreview
  base="react-email"
  name="cta-with-background-image-demo"
  variant="padded"
  hideCode
/>

## API Reference

| Prop                                     | Type                             | Default          | Description                                       |
| ---------------------------------------- | -------------------------------- | ---------------- | ------------------------------------------------- |
| `theme`                                  | `TailwindConfig`                 | `defaultTheme`   | React Email Tailwind configuration                |
| `variant`                                | `"flush" \| "boxed" \| "padded"` | `"flush"`        | Selects the exact Mailviews layout and copy       |
| `heading` / `subtext` / `emphasis`       | `string`                         | Variant content  | CTA copy; `emphasis` renders bold inline text     |
| `ctaLabel` / `ctaHref`                   | `string`                         | Variant CTA      | Primary action label and destination              |
| `secondaryCtaLabel` / `secondaryCtaHref` | `string`                         | Variant CTA      | Secondary action label and destination            |
| `backgroundSrc`                          | `string`                         | Variant image    | Background image URL                              |
| Color props                              | `string`                         | Mailviews colors | Canvas, surface, overlay, text, and button colors |
