---
title: "Container"
description: "Layout container for wrapping email content with responsive padding."
---

<ComponentPreview base="mjml-react" name="container-flush-mobile-demo" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @emailcn/container
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @faire/mjml-react mjml
```

<Step>Copy the component source into your project.</Step>

<ComponentSource
  src="registry/bases/mjml-react/ui/ui-elements/containers/container.tsx"
  base="mjml-react"
  title="components/email/container.tsx"
/>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { Container } from "@/components/email/container";
```

```tsx
<Container mobile="gutters">{children}</Container>
<Container mobile="flush">{children}</Container>
```

## Examples

### Gutters on Mobile

<ComponentPreview base="mjml-react" name="container-gutters-mobile-demo" />

## API Reference

| Prop       | Type                            | Default        | Description                     |
| ---------- | ------------------------------- | -------------- | ------------------------------- |
| `mobile`   | `"gutters" \| "flush"`          | `"gutters"`    | Side padding behavior on mobile |
| `align`    | `"left" \| "center" \| "right"` | `"center"`     | Horizontal alignment            |
| `maxWidth` | `string`                        | `"600px"`      | Maximum container width         |
| `children` | `React.ReactNode`               | -              | Container content               |
| `content`  | `React.ReactNode`               | -              | Alternate content prop          |
| `theme`    | `EmailThemeTokens`              | `defaultTheme` | Email theme styling             |
