---
title: "Split Full Bleed"
description: "A full-bleed product hero with image-left and image-right layouts"
---

<ComponentPreview base="mjml-react" name="hero-split-full-bleed-demo" />

## Installation

<CodeTabs>
<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @emailcn/hero-split-full-bleed
```

</TabsContent>
<TabsContent value="manual">

<Steps>
  <Step>Install the following dependencies:</Step>

```bash
npm install @faire/mjml-react
```

<Step>Copy the component source into your project.</Step>
  <ComponentSource
    src="registry/bases/mjml-react/ui/marketing/hero/hero-split-full-bleed.tsx"
    base="mjml-react"
    title="components/emails/marketing/hero/hero-split-full-bleed.tsx"
  />
</Steps>

</TabsContent>
</CodeTabs>

## Usage

```tsx
import { HeroSplitFullBleed } from "@/components/emails/marketing/hero/hero-split-full-bleed";
```

```tsx
<HeroSplitFullBleed {...HeroSplitFullBleed.PreviewProps} />
```

## Examples

### Image Left

<ComponentPreview
  base="mjml-react"
  name="hero-split-full-bleed-demo"
  variant="image-left"
  hideCode
/>

### Image Right

<ComponentPreview
  base="mjml-react"
  name="hero-split-full-bleed-demo"
  variant="image-right"
  hideCode
/>

## API Reference

| Prop                    | Type                            | Default           | Description                                      |
| ----------------------- | ------------------------------- | ----------------- | ------------------------------------------------ |
| `theme`                 | `EmailThemeTokens`              | `defaultTheme`    | MJML React theme tokens                          |
| `variant`               | `"image-left" \| "image-right"` | `"image-left"`    | Places the full-bleed image on the selected side |
| `eyebrow`               | `string`                        | `"Coffee"`        | Small label above the heading                    |
| `heading`               | `string`                        | `"Sarina"`        | Main product heading                             |
| `subheading`            | `string`                        | `"Americano…"`    | Supporting line below the heading                |
| `description`           | `string`                        | Mailviews copy    | Product description                              |
| `price`                 | `string`                        | `"From $14.99"`   | Bold price line; pass an empty string to hide it |
| `ctaLabel` / `ctaHref`  | `string`                        | Mailviews CTA     | Button label and destination                     |
| `imageSrc` / `imageAlt` | `string`                        | Mailviews imagery | Full-bleed background image and accessible label |
| `logoSrc` / `logoAlt`   | `string`                        | Mailviews logo    | Logo displayed over the image                    |
| `textBackgroundColor`   | `string`                        | `"#fffffe"`       | Text-panel background                            |
| `pageBackgroundColor`   | `string`                        | `"#f1f5f9"`       | Email-canvas background                          |
| `buttonBackgroundColor` | `string`                        | `"#4f46e5"`       | CTA background                                   |
| `buttonTextColor`       | `string`                        | `"#fffffe"`       | CTA text color                                   |
