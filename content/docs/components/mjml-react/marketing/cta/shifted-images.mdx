---
title: "Shifted Images"
description: "An image-led CTA with flush-side, alternating-offset, and collage variants"
---

<ComponentPreview base="mjml-react" name="cta-with-shifted-images-demo" />

## Installation

<CodeTabs>
<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @emailcn/cta-with-shifted-images
```

</TabsContent>
<TabsContent value="manual">

<Steps>
  <Step>Install the following dependencies:</Step>

```bash
npm install @faire/mjml-react mjml
```

<Step>Copy the component source into your project.</Step>
  <ComponentSource
    src="registry/bases/mjml-react/ui/marketing/cta/cta-with-shifted-images.tsx"
    base="mjml-react"
    title="components/email/cta-with-shifted-images.tsx"
  />
</Steps>

</TabsContent>
</CodeTabs>

## Usage

```tsx
import { CTAWithShiftedImages } from "@/components/email/cta-with-shifted-images";
```

```tsx
<CTAWithShiftedImages {...CTAWithShiftedImages.PreviewProps} />
```

## Examples

### Images Offset

<ComponentPreview
  base="mjml-react"
  name="cta-with-shifted-images-demo"
  variant="images-offset"
/>

### Images Offset Alt

<ComponentPreview
  base="mjml-react"
  name="cta-with-shifted-images-demo"
  variant="images-offset-alt"
/>

### Collage

<ComponentPreview
  base="mjml-react"
  name="cta-with-shifted-images-demo"
  variant="collage"
/>

## API Reference

| Prop                      | Type                          | Default               | Description                                 |
| ------------------------- | ----------------------------- | --------------------- | ------------------------------------------- |
| `theme`                   | `EmailThemeTokens`            | `defaultTheme`        | React Email Tailwind configuration          |
| `variant`                 | `CTAWithShiftedImagesVariant` | `"flush-side-images"` | Selects the exact layout and assets         |
| `heading` / `subtext`     | `string`                      | Variant copy          | CTA heading and supporting text             |
| `ctaLabel` / `ctaHref`    | `string`                      | CTA                   | Primary action label and destination        |
| `imageSrc1` … `imageSrc4` | `string`                      | Variant images        | Image URLs used by the selected composition |
| `imageAlt1` … `imageAlt4` | `string`                      | `""`                  | Alternative text for each image             |
| Color props               | `string`                      | colors                | Canvas, surface, text, and button colors    |
