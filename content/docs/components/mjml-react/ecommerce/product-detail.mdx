---
title: "Product Detail"
description: "Product detail displays with images, descriptions, and purchase options."
---

## Feature with Product Image

<ComponentPreview base="mjml-react" name="feature-with-product-image-demo" />

### Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @emailcn/feature-with-product-image
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @faire/mjml-react
```

<Step>Copy the component source into your project.</Step>

<ComponentSource
  src="registry/bases/mjml-react/ui/ecommerce/product-detail/feature-with-product-image.tsx"
  base="mjml-react"
  title="components/emails/ecommerce/product-detail/feature-with-product-image.tsx"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

### Usage

```tsx
import { FeatureWithProductImage } from "@/components/emails/ecommerce/product-detail/feature-with-product-image";
```

```tsx
<FeatureWithProductImage {...FeatureWithProductImage.PreviewProps} />
```

### API Reference

| Prop                 | Type         | Default        | Description         |
| -------------------- | ------------ | -------------- | ------------------- |
| `productImageUrl`    | `string`     | -              | Product image URL   |
| `productName`        | `string`     | -              | Product name        |
| `productDescription` | `string`     | -              | Product description |
| `price`              | `string`     | -              | Product price       |
| `ctaLabel`           | `string`     | -              | CTA button label    |
| `ctaHref`            | `string`     | `"#"`          | CTA button link     |
| `theme`              | `EmailTheme` | `defaultTheme` | Email theme styling |

## Two Columns Image Grid with Full Width Feature

<ComponentPreview
  base="mjml-react"
  name="two-columns-image-grid-with-full-width-feature-demo"
/>

### Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @emailcn/two-columns-image-grid-with-full-width-feature
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @faire/mjml-react
```

<Step>Copy the component source into your project.</Step>

<ComponentSource
  src="registry/bases/mjml-react/ui/ecommerce/product-detail/two-columns-image-grid-with-full-width-feature.tsx"
  base="mjml-react"
  title="components/emails/ecommerce/product-detail/two-columns-image-grid-with-full-width-feature.tsx"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

### Usage

```tsx
import { TwoColumnsImageGridWithFullWidthFeature } from "@/components/emails/ecommerce/product-detail/two-columns-image-grid-with-full-width-feature";
```

```tsx
<TwoColumnsImageGridWithFullWidthFeature {...TwoColumnsImageGridWithFullWidthFeature.PreviewProps} />
```

### API Reference

| Prop                 | Type         | Default        | Description         |
| -------------------- | ------------ | -------------- | ------------------- |
| `productImageUrl`    | `string`     | -              | Product image URL   |
| `productName`        | `string`     | -              | Product name        |
| `productDescription` | `string`     | -              | Product description |
| `price`              | `string`     | -              | Product price       |
| `ctaLabel`           | `string`     | -              | CTA button label    |
| `ctaHref`            | `string`     | `"#"`          | CTA button link     |
| `theme`              | `EmailTheme` | `defaultTheme` | Email theme styling |

## Bento Grid with Images and Details

<ComponentPreview
  base="mjml-react"
  name="bento-grid-with-images-and-details-demo"
/>

### Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @emailcn/bento-grid-with-images-and-details
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @faire/mjml-react
```

<Step>Copy the component source into your project.</Step>

<ComponentSource
  src="registry/bases/mjml-react/ui/ecommerce/product-detail/bento-grid-with-images-and-details.tsx"
  base="mjml-react"
  title="components/emails/ecommerce/product-detail/bento-grid-with-images-and-details.tsx"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

### Usage

```tsx
import { BentoGridWithImagesAndDetails } from "@/components/emails/ecommerce/product-detail/bento-grid-with-images-and-details";
```

```tsx
<BentoGridWithImagesAndDetails {...BentoGridWithImagesAndDetails.PreviewProps} />
```

### API Reference

| Prop                 | Type         | Default        | Description         |
| -------------------- | ------------ | -------------- | ------------------- |
| `productImageUrl`    | `string`     | -              | Product image URL   |
| `productName`        | `string`     | -              | Product name        |
| `productDescription` | `string`     | -              | Product description |
| `price`              | `string`     | -              | Product price       |
| `ctaLabel`           | `string`     | -              | CTA button label    |
| `ctaHref`            | `string`     | `"#"`          | CTA button link     |
| `theme`              | `EmailTheme` | `defaultTheme` | Email theme styling |

## Two Column Team Cards with Details

<ComponentPreview
  base="mjml-react"
  name="two-column-team-cards-with-details-demo"
/>

### Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @emailcn/two-column-team-cards-with-details
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @faire/mjml-react
```

<Step>Copy the component source into your project.</Step>

<ComponentSource
  src="registry/bases/mjml-react/ui/ecommerce/product-detail/two-column-team-cards-with-details.tsx"
  base="mjml-react"
  title="components/emails/ecommerce/product-detail/two-column-team-cards-with-details.tsx"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

### Usage

```tsx
import { TwoColumnTeamCardsWithDetails } from "@/components/emails/ecommerce/product-detail/two-column-team-cards-with-details";
```

```tsx
<TwoColumnTeamCardsWithDetails {...TwoColumnTeamCardsWithDetails.PreviewProps} />
```

### API Reference

| Prop                 | Type         | Default        | Description         |
| -------------------- | ------------ | -------------- | ------------------- |
| `productImageUrl`    | `string`     | -              | Product image URL   |
| `productName`        | `string`     | -              | Product name        |
| `productDescription` | `string`     | -              | Product description |
| `price`              | `string`     | -              | Product price       |
| `ctaLabel`           | `string`     | -              | CTA button label    |
| `ctaHref`            | `string`     | `"#"`          | CTA button link     |
| `theme`              | `EmailTheme` | `defaultTheme` | Email theme styling |

## Split Product Detail

<ComponentPreview base="mjml-react" name="split-product-detail-demo" />

### Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @emailcn/split-product-detail
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @faire/mjml-react
```

<Step>Copy the component source into your project.</Step>

<ComponentSource
  src="registry/bases/mjml-react/ui/ecommerce/product-detail/split-product-detail.tsx"
  base="mjml-react"
  title="components/emails/ecommerce/product-detail/split-product-detail.tsx"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

### Usage

```tsx
import { SplitProductDetail } from "@/components/emails/ecommerce/product-detail/split-product-detail";
```

```tsx
<SplitProductDetail {...SplitProductDetail.PreviewProps} />
```

### API Reference

| Prop                 | Type         | Default        | Description         |
| -------------------- | ------------ | -------------- | ------------------- |
| `productImageUrl`    | `string`     | -              | Product image URL   |
| `productName`        | `string`     | -              | Product name        |
| `productDescription` | `string`     | -              | Product description |
| `price`              | `string`     | -              | Product price       |
| `ctaLabel`           | `string`     | -              | CTA button label    |
| `ctaHref`            | `string`     | `"#"`          | CTA button link     |
| `theme`              | `EmailTheme` | `defaultTheme` | Email theme styling |