---
title: "Team Avatars"
description: "A people-focused CTA with grouped, top-aligned, and single-avatar variants"
---

<ComponentPreview base="jsx-email" name="cta-with-team-avatars-demo" />

## Installation

<CodeTabs>
<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @emailcn/cta-with-team-avatars
```

</TabsContent>
<TabsContent value="manual">

<Steps>
  <Step>Install the following dependencies:</Step>

```bash
npm install jsx-email
```

<Step>Copy the component source into your project.</Step>
  <ComponentSource
    src="registry/bases/jsx-email/ui/marketing/cta/cta-with-team-avatars.tsx"
    base="jsx-email"
    title="components/email/cta-with-team-avatars.tsx"
  />
</Steps>

</TabsContent>
</CodeTabs>

## Usage

```tsx
import { CTAWithTeamAvatars } from "@/components/email/cta-with-team-avatars";
```

```tsx
<CTAWithTeamAvatars {...CTAWithTeamAvatars.PreviewProps} />
```

## Examples

### Avatars Top

<ComponentPreview
  base="jsx-email"
  name="cta-with-team-avatars-demo"
  variant="avatars-top"
/>

### Single Avatar

<ComponentPreview
  base="jsx-email"
  name="cta-with-team-avatars-demo"
  variant="single-avatar"
/>

## API Reference

| Prop                                     | Type                                            | Default           | Description                                             |
| ---------------------------------------- | ----------------------------------------------- | ----------------- | ------------------------------------------------------- |
| `theme`                                  | `EmailThemeTokens`                              | `defaultTheme`    | JSX Email theme tokens                                  |
| `variant`                                | `"default" \| "avatars-top" \| "single-avatar"` | `"default"`       | Selects the exact layout and copy                       |
| `heading` / `subtext`                    | `string`                                        | Variant copy      | CTA heading and supporting text                         |
| `ctaLabel` / `ctaHref`                   | `string`                                        | Variant CTA       | Primary action label and destination                    |
| `secondaryCtaLabel` / `secondaryCtaHref` | `string`                                        | Single-avatar CTA | Optional secondary action                               |
| Avatar props                             | `string`                                        | avatars           | Four avatar URLs and alternative-text values            |
| Color props                              | `string`                                        | colors            | Canvas, surface, avatar border, text, and button colors |
