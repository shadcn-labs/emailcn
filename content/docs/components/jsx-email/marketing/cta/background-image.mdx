---
title: "Background Image"
description: "A background-image CTA with flush, boxed-overlay, and padded-card variants"
---

<ComponentPreview base="jsx-email" name="cta-with-background-image-demo" />

## Installation

<CodeTabs>
<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @emailcn/cta-with-background-image
```

</TabsContent>
<TabsContent value="manual">

<Steps>
  <Step>Install the following dependencies:</Step>

```bash
npm install jsx-email
```

<Step>Copy the component source into your project.</Step>
  <ComponentSource
    src="registry/bases/jsx-email/ui/marketing/cta/cta-with-background-image.tsx"
    base="jsx-email"
    title="components/email/cta-with-background-image.tsx"
  />
</Steps>

</TabsContent>
</CodeTabs>

## Usage

```tsx
import { CTAWithBackgroundImage } from "@/components/email/cta-with-background-image";
```

```tsx
<CTAWithBackgroundImage {...CTAWithBackgroundImage.PreviewProps} />
```

## Examples

### Boxed

<ComponentPreview
  base="jsx-email"
  name="cta-with-background-image-demo"
  variant="boxed"
/>

### Padded

<ComponentPreview
  base="jsx-email"
  name="cta-with-background-image-demo"
  variant="padded"
/>

## API Reference

| Prop                                     | Type                             | Default         | Description                                       |
| ---------------------------------------- | -------------------------------- | --------------- | ------------------------------------------------- |
| `theme`                                  | `EmailThemeTokens`               | `defaultTheme`  | JSX Email theme tokens                            |
| `variant`                                | `"flush" \| "boxed" \| "padded"` | `"flush"`       | Selects the exact layout and copy                 |
| `heading` / `subtext` / `emphasis`       | `string`                         | Variant content | CTA copy; `emphasis` renders bold inline text     |
| `ctaLabel` / `ctaHref`                   | `string`                         | Variant CTA     | Primary action label and destination              |
| `secondaryCtaLabel` / `secondaryCtaHref` | `string`                         | Variant CTA     | Secondary action label and destination            |
| `backgroundSrc`                          | `string`                         | Variant image   | Background image URL                              |
| Color props                              | `string`                         | colors          | Canvas, surface, overlay, text, and button colors |
