---
title: "Overlayed Split"
description: "A split background-image hero with a translucent content overlay"
---

<ComponentPreview base="jsx-email" name="hero-overlayed-split-demo" />

## Installation

<CodeTabs>
<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @emailcn/hero-overlayed-split
```

</TabsContent>
<TabsContent value="manual">

<Steps>
  <Step>Install the following dependencies:</Step>

```bash
npm install jsx-email
```

<Step>Copy the component source into your project.</Step>
  <ComponentSource
    src="registry/bases/jsx-email/ui/marketing/hero/hero-overlayed-split.tsx"
    base="jsx-email"
    title="components/email/hero-overlayed-split.tsx"
  />
</Steps>

</TabsContent>
</CodeTabs>

## Usage

```tsx
import { HeroOverlayedSplit } from "@/components/email/hero-overlayed-split";
```

```tsx
<HeroOverlayedSplit {...HeroOverlayedSplit.PreviewProps} />
```

## Examples

### Overlay Right

<ComponentPreview
  base="jsx-email"
  name="hero-overlayed-split-demo"
  variant="overlay-right"
/>

## API Reference

| Prop                    | Type                                | Default          | Description                                      |
| ----------------------- | ----------------------------------- | ---------------- | ------------------------------------------------ |
| `theme`                 | `EmailThemeTokens`                  | `defaultTheme`   | React Email Tailwind configuration               |
| `variant`               | `"overlay-left" \| "overlay-right"` | `"overlay-left"` | Places the translucent content panel on one side |
| `eyebrow`               | `string`                            | `"Brew Stories"` | Small label above the heading                    |
| `heading`               | `string`                            | copy             | Main article heading                             |
| `subheading`            | `string`                            | `"January…"`     | Date or supporting line                          |
| `description`           | `string`                            | copy             | Supporting paragraph                             |
| `ctaLabel` / `ctaHref`  | `string`                            | CTA              | Button label and destination                     |
| `imageSrc` / `imageAlt` | `string`                            | imagery          | Full-width background image and label            |
| `logoSrc` / `logoAlt`   | `string`                            | Light logo       | Logo shown at the top of the overlay             |
| `logoHref`              | `string`                            | Example URL      | Logo destination                                 |
| `pageBackgroundColor`   | `string`                            | `"#f1f5f9"`      | Email-canvas background                          |
| `overlayColor`          | `string`                            | Dark 80% overlay | Content-panel background                         |
| `textColor`             | `string`                            | `"#f9fafb"`      | Overlay text color                               |
| `buttonBackgroundColor` | `string`                            | `"#4f46e5"`      | CTA background                                   |
| `buttonTextColor`       | `string`                            | `"#fffffe"`      | CTA text color                                   |
