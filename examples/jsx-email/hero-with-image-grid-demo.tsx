import { HeroWithImageGrid } from "@/registry/bases/jsx-email/ui/marketing/hero/hero-with-image-grid";

export default function HeroWithImageGridDemo() {
  return <HeroWithImageGrid {...HeroWithImageGrid.PreviewProps} />;
}
